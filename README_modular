# 🚀 Модульная архитектура скрипта

## 📊 Сравнение размеров:
- **Оригинальный файл**: 6,771 строк → **Ошибка "Out of local registers"**
- **Модульная версия**: 1,450 строк общего кода, разделенного на модули
- **Основной загрузчик**: всего 230 строк → **Никаких проблем с registers!**

## 🎯 Решение проблемы
Проблема "Out of local registers when trying to allocate createDropdown: exceeded limit 200" была решена путем разделения одного огромного скрипта на отдельные модули, которые загружаются по требованию через `loadstring` и GitHub.

## 📁 Структура модулей:

### 1. **huyna_modular** (230 строк) - Основной загрузчик
- Система загрузки модулей через GitHub
- Кэширование модулей
- Система событий между модулями
- Утилиты для модулей

### 2. **autofarm_module** (469 строк) - Модуль автофарма
- ✅ Улучшенное отслеживание позиций через `BodyPosition`
- ✅ Динамическое следование за движущимися предметами
- ✅ Исправленный отступ -2 единицы
- ✅ Трёхэтапное движение с fallback логикой

### 3. **movement_module** (507 строк) - Модуль движения
- NoClip, Fly, SpeedHack
- LongJump, InfiniteJump
- Система событий для связи с автофармом

### 4. **gui_module** (244 строки) - Простой GUI
- Интерфейс для загрузки модулей
- Статус модулей в реальном времени
- Хоткей Insert для показа/скрытия

## 🔧 Как использовать:

### Шаг 1: Загрузите модули на GitHub
Создайте репозиторий на GitHub и загрузите файлы:
- `modules/gui_module.lua`
- `modules/autofarm_module.lua`
- `modules/movement_module.lua`
- `modules/yba_module.lua` (нужно создать)
- `modules/esp_module.lua` (нужно создать)

### Шаг 2: Обновите URL в основном загрузчике
В файле `huyna_modular` измените строку:
```lua
local GITHUB_BASE_URL = "https://raw.githubusercontent.com/ВАШЕ_ИМЯ/ВАШ_РЕПО/main/modules/"
```

### Шаг 3: Запустите основной загрузчик
```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/ВАШЕ_ИМЯ/ВАШ_РЕПО/main/huyna_modular.lua"))()
```

## 💡 Команды для отладки:

```lua
-- Загрузить модуль
_G.ml("Movement")  -- или _G.loadModuleOnDemand("Movement")

-- Перезагрузить модуль
_G.mr("Autofarm")

-- Показать все модули
_G.mlist()

-- Статус системы
_G.mstatus()
```

## 🎮 Управление:

### GUI:
- **Insert** - Показать/скрыть интерфейс
- Кликайте по кнопкам модулей для их загрузки

### Autofarm:
```lua
_G.AutofarmModule.start()  -- Запустить
_G.AutofarmModule.stop()   -- Остановить
_G.AutofarmModule.toggle() -- Переключить
```

### Movement:
```lua
_G.MovementModule.noClip(true)    -- Включить NoClip
_G.MovementModule.fly(true)       -- Включить полет
_G.MovementModule.speedHack(true) -- Включить ускорение
```

## ⚡ Преимущества модульной архитектуры:

1. **Решает проблему local registers** - каждый модуль загружается отдельно
2. **Быстрая загрузка** - загружаются только нужные модули
3. **Легкое обновление** - можно обновлять модули независимо
4. **Кэширование** - модули загружаются только один раз
5. **Отладка** - проще найти и исправить ошибки в отдельных модулях
6. **Масштабируемость** - легко добавлять новые модули

## 🔄 Система событий:

Модули могут общаться друг с другом через события:
```lua
-- Отправить событие
_G.ModuleEvents.fire("AutofarmStarted")

-- Подписаться на событие
_G.ModuleEvents.connect("RequestNoClip", function(enabled)
    MovementModule.noClip(enabled)
end)
```

## 🆕 Улучшения автофарма:

- **Динамическое отслеживание**: Персонаж следует за движущимися предметами
- **BodyPosition поддержка**: Правильное определение позиции предметов
- **Исправленный отступ**: Точно -2 единицы от предмета
- **Fallback логика**: Если BodyPosition не найден, используется старая логика

## 📈 Результат:

✅ **Проблема "Out of local registers" решена**
✅ **Автофарм работает лучше с динамическим отслеживанием**
✅ **Код стал модульным и поддерживаемым**
✅ **Легко добавлять новые функции**
✅ **Быстрая загрузка и кэширование**

---

💡 **Совет**: Начните с загрузки GUI модуля, а затем загружайте остальные модули по мере необходимости!